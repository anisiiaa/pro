#include <iostream>
using namespace std;

int main()
{
    setlocale(LC_ALL, "rus");
    int a;
    char b, c;
    cout << "Сколько сейчас времени? \n";
    cin >> a;
    cout << "Шторы раздвинуты? \n";
    cin >> b;
    cout << "Лампа включена? \n";
    cin >> c;
    if (((a > 5) && (a < 20) && (b == '+')) || (c == '+'))
        cout << "Светло";
    else
        cout << "Темно";
}
