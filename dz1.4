#include <iostream>
using namespace std;

int main()
{
    setlocale(LC_ALL, "rus");
    float a, b, c, d;
    cout << "Введите три числа: \n";
    cin >> a >> b >> c;
    if (a == 0)
    {   
        if (b == 0 && c != 0)
            cout << "нет решений";
        if (b == 0 && c == 0)
            cout << "x - любой";
        return 1;
    }
    d = b * b - (4 * c * a);
    if (d >= 0)
    {
        if (d > 0)
            cout << "Первый корень:" << (-b + sqrt(d)) / (2 * a) << endl << "Второй корень:" << (-b - sqrt(d)) / (2 * a);
        else
            cout << "Один корень:" << (-b /( 2 * a));
    }
    else
        cout << "нет корней";
}
