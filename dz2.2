#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	setlocale(LC_ALL, "rus");
	float a, x;
    cout << "Введите два числа: \n";
	cin >> a >> x;
    if (a == 0)
    {
        cout  << "Функция не определена при данных значениях" << endl;
        return 1;
    }
    if (abs (x) < 1)
        cout << "Результат:" << a * log (abs(x)) << endl;
    else
        cout << "Результат:" << sqrt (a - x*x) << endl;
	
	}
