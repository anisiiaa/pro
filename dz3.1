#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	setlocale(LC_ALL, "rus");
	float a, x;
    cout << "Введите два числа: \n";
	cin >> a >> x;
    if (abs (x) < 1 && x > 0) 
     cout << "Результат:" << a * log (x) << endl;
    else
    {
        if  (abs(x)>= 1 && a > x*x)
            cout << "Результат:" << sqrt (a - x * x) << endl;
        else
        cout  << "Функция не определена при данных значениях" << endl;
    }
